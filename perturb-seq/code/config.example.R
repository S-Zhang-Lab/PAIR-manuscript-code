# config.example.R — copy this file to config.local.R and edit DATA_ROOT.
#
# config.local.R is gitignored so each user/machine has its own path.
# config.R (in this directory) sources config.local.R automatically; you
# do not need to source this file directly.

DATA_ROOT <- "/path/to/your/PAIR-perturb-seq/data-root"
